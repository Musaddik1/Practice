package com.example.teacher;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.ComponentScan;
import org.springframework.context.annotation.Configuration;

/*@Configuration
@ComponentScan(basePackages = "com.example.teacher")*/
public class CollegeConfig {

	/*
	 * @Bean public College collegeBean() { College college=new College();
	 * 
	 * college.setTeacher(teacherBean()); college.setTeacher(scienceBean());
	 * 
	 * //college.setAddress(addressBean()); return college;
	 * 
	 * }
	 * 
	 * @Bean public Teacher teacherBean() { Teacher teacher=new MathTeacher();
	 * return teacher; }
	 * 
	 * public Teacher scienceBean() { Teacher teacher=new ScienceTeacher(); return
	 * teacher; } public Address addressBean() { Address address=new Address();
	 * address.setCity("pune"); return address; }
	 */
}
